python_version: "3.10"
model_id: "eos3b5e"
model_ids: ["eos3b5e", "eos9gg2"]
input_file: "files/input.csv"
output_file: "files/output_eos9gg2_0.json"              # "files/result.csv" & "files/output_eos9gg2_0.json"  # Leave blank if you don’t need output
output_redirection: true                          # Enable redirection of command output to a file
max_runtime_minutes: 5                            # applied for the run command only
fetch_flags: "--from_dockerhub"                   # Add any optional flags here; leave empty if not needed. eg --from_dockerhub
serve_flags: ""
run_flags: ""
overwrite_ersilia_repo: false                     # Set to true to overwrite if repo exists
use_existing_env: true
delete_model: false
activate_docker: true
runner: "single"                                  # multiple for multiple
cli_type: "all"                                   # cli type: fetch, serve, run, close, delete
log_error: true
log_path: "files/error_log"                       # current dir for instance: no extention(.txt)